import { LastOnlinePipe } from './last-online.pipe';

describe('LastOnlinePipe', () => {
  it('create an instance', () => {
    const pipe = new LastOnlinePipe();
    expect(pipe).toBeTruthy();
  });
});
